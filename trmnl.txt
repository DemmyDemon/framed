[X] Yank out the TUI.
[X] Just read from the file.
[X] Simplify greatly by just using the
    file mtime.
[X] Blindly generate the image, trust the
    mtime, and it being LAN locked.
[X] Free up the first line, since we don't
    use the day/week display there anymore.
[ ] Re-adjust everything to use that line
    for data from the actual text file.
[ ] Celebrate a more useful and systemd
    friendly version!
[ ] Regret the whole change and breathe a
    sigh of relief it's all in a git repo.
[X] Make sure overflow still works as it should, both in length and width.
[X] Make the display "scroll" if the file
    is longer than fits. Log display,
    baby! Yeah!
